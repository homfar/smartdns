services:
  geodns:
    build:
      context: .
      args:
        GO_IMAGE: ${GO_IMAGE:-golang:1.22}
        RUNTIME_IMAGE: ${RUNTIME_IMAGE:-alpine:3.20}
    env_file: .env
    ports: ["53:53/udp","53:53/tcp","5555:5555"]
    volumes: ["./data:/data","./mmdb:/mmdb"]
